# Render.com blueprint: New + -> Blueprint -> pick this repo, and it creates the service.
services:
  - type: web
    name: openfront-lite
    runtime: node
    plan: free
    buildCommand: npm ci
    startCommand: npm start
    healthCheckPath: /healthz
    envVars:
      - key: NODE_ENV
        value: production
